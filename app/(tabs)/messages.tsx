import { PhasePlaceholder } from "@/components/PhasePlaceholder";

export default function MessagesScreen() {
  return (
    <PhasePlaceholder
      title="Messages — Phase 2"
      body="Direct messages and conversation threads will appear here in Phase 2."
    />
  );
}
