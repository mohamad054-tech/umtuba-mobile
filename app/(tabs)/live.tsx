import { PhasePlaceholder } from "@/components/PhasePlaceholder";

export default function LiveScreen() {
  return (
    <PhasePlaceholder
      title="Live lobby — Phase 2"
      body="Live rooms, lobby discovery, and LiveKit playback will land here. Optional EXPO_PUBLIC_LIVEKIT_URL is already wired in env validation."
    />
  );
}
